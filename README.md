# Fedora Zimablue

Minimal changes to Fedora Silverblue using the UBlue image template repository.

### Changes

- Preinstalled dnf packages:
  - tmux
  - fish
  - nvim
  - distrobox
- Brew is installed by default, under /var/brew
  - Add users to group `brew` to give them write access
